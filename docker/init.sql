CREATE TABLE grpc_data (
    PacketSeqNum INT,
    RecordSeqNum INT,
    PacketTimestamp TIMESTAMP,
    Decimal1 DECIMAL,
    Decimal2 DECIMAL,
    Decimal3 DECIMAL,
    Decimal4 DECIMAL,
    RecordTimestamp TIMESTAMP
);
